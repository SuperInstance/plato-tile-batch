[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-batch"
version = "0.1.0"
description = "Bulk tile processing — partition, filter, dedup"
license = {text = "MIT"}
requires-python = ">=3.8"
